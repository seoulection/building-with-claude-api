[project]
name = "claude-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "seoulection", email = "6445410+seoulection@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.122.0",
    "python-dotenv>=1.2.3",
]

[project.scripts]
claude-api = "claude_api:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
